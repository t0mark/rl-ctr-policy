# 작업 규칙

- claude code의 메모리 기능 쓰지 않는다.
- git 명령어는 사용하지 않는다.
- 파일을 생성할 때, 파일명, 폴더 구조부터 제안한다. 내용부터 작성하지 않는다.
    - 임시 파일은 이 규칙을 적용하지 않는다. (단, 사용 후 반드시 삭제)
- 모든 작업 단계에서 파일럿 테스트를 진행한다.
    - 파일럿 테스트에서 시각화 결과물을 산출하여, 사용자가 점검할 수 있게 만든다.
- 전체 설계에 초점을 맞추고 작업한다.
    - 간략화한 작업부터 시작해서 확장해나가는 구조를 사용하지 않는다.
    - 파일럿 테스트는 기본 기능만 구현하고 확장해 나가는 구조가 아니다. 전체 실행을 하기 전에, 로직의 오류가 없는지 검증하는 테스트이다.
- 파일을 수정할 때, 일부만 수정하는 게 아니라 처음부터 다시 작성한다. 편집 흔적을 남기지 않는다.
- **디버깅할 때, 코드를 읽고 구조와 로직을 파악하여 분석한다. 디버깅용 파일을 만들지 않는다.**

## 코드 작성 규칙

- 각 클래스는 하나의 책임만 가진다. (SOLID의 단일 책임 원칙 준수)
- 속성은 private/protected 관례(_ 또는 __)를 사용해 캡슐화하고, 필요하면 getter/setter(@property)를 쓴다.
- 프로세스 실행 중 상태를 확인할 수 있도록 로그를 남기도록 코드를 작성한다.
- CPU 사용 최소화, GPU 사용을 최대화하도록 코드를 작성한다.
- 변수명은 변수가 어떤 기능을 하는지 알아볼 수 있도록 정한다.
- 절대 경로 사용하지 않는다. 이식성을 고려하여 코드를 작성한다.
- 주석 작성 규칙
    - 주석은 한국어로 작성한다.
    - 모든 클래스·함수에 docstring을 작성한다.
    - 논리 블록 단위로 주석을 작성한다. 기능을 쉽고 간결하게 1줄로 설명한다.
    - 주석은 코드 위에 작성한다. (코드 옆에 작성 X)
    - 수정 사항을 주석으로 작성하지 않는다. 반드시 기능 설명만 작성한다.
- 코드에서 에러 발생 시, 문제를 우회하지 않고 문제가 되는 부분을 분석하여 직접적으로 해결한다.
- 사용자 요청 없이 코드를 직접 실행하지 않는다. 코드 작성 후, 실행 명령어만 알려준다.
    - 임시 파일은 이 규칙을 적용하지 않는다.

## Docker 관련 규칙

- 모든 프로세스는 Docker 컨테이너 내부에서 실행한다.
- 컨테이너 이름은 `airlab_hw_{컨테이너 이름}`, 이미지 이름은 `t0mark251/{이미지 이름}`으로 작성한다.
- 도커 실행 및 빌드 관리는 compose를 사용한다.
- 초기 도커 파일 검증 방법 : 빌드는 최소한으로 진행
    1. Dockerfile 초안 작성
    2. 본 실행 및 컨테이너에서 수행할 작업 테스트
    3. 의존성 문제등 발생 시, 컨테이너 내부에서 해결 (Dockerfile 수정 X)
    4. 모든 의존성 문제 해결 완료 후, 도커 파일에 반영
